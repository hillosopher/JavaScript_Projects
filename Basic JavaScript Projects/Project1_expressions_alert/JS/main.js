let days = 42;
let x = `It's been ` + days + ` since the accident`;
// window.alert(x);
// alert(sum);

// THis is a comment

let sent1 = `This is the beginning of the string`;
let sent2 = `and this is the end of the string`;
const sentConcat = sent1 + sent2; // This concats stuff
let sum = 3 + 3; // This adds stuff

function onClick() {
  let button = document.getElementById("button");
  button.innerHTML = "Sup";
}
